SELECT MAX(age) FROM users; -- This query selects the maximum age from the 'users' table.

SELECT MIN(age) FROM users; -- This query selects the minimum age from the 'users' table.

/* Just return 1 value but you can use it in a subquery to get more values */