-- Comment in a single line

/* Comment 
but 
in 
multiple lines */

SELECT * FROM hello_mysql.users; -- This query selects all columns from the 'users' table in the 'hello_mysql' database.
SELECT name FROM hello_mysql.users; -- This query selects only the 'name' column from the 'users' table in the 'hello_mysql' database.
SELECT user_id, name FROM hello_mysql.users; 

